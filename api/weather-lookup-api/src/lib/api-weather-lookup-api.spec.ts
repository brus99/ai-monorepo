import { apiWeatherLookupApi } from './api-weather-lookup-api';

describe('apiWeatherLookupApi', () => {
  it('should work', () => {
    expect(apiWeatherLookupApi()).toEqual('api-weather-lookup-api');
  });
});
